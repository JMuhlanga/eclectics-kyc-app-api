# Application name
spring.application.name=eclectics-kyc-app-api

# Server Configuration
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/kycBankingApp
spring.datasource.username="kupekadev"
spring.datasource.password="@Wr8xsd6k"
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=none  # Ensures it doesn't alter your manually created schema
spring.jpa.show-sql=true            # Logs SQL queries
spring.jpa.properties.hibernate.format_sql=true

# Hibernate Dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Other Configurations
spring.mvc.pathmatch.matching-strategy=ant_path_matcher
